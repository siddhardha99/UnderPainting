# ADR-006: Working name "Underpainting"

**Status:** Proposed — pending human verification (do not resolve in code)

## Decision (pending)

The working name is **Underpainting**; `underpainting` is the intended Marketplace and npm ID.

## Blockers before first publish

- Marketplace publisher + extension ID availability
- npm package name availability
- Trademark search

Until verified, `package.json` keeps `"private": true` and the placeholder publisher. Tracked in docs/OPEN_QUESTIONS.md #1.
