# Dev sandbox

The F5 launch config opens the Extension Development Host on **this folder**, so generated versions always have a workspace to persist into (`sandbox/.design/`, gitignored) — frames, chat history, restore, and refinement all depend on that.

`demo-theme.css` exists to exercise the DesignSystemExtractor: run **Underpainting: Extract Design System** here and subsequent generations are grounded in its tokens.
