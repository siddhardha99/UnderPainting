# Evals

The golden-prompt eval harness arrives with **M1 item 10** (BUILD_BRIEF.md §11): golden prompts scored by deterministic structural checks (valid HTML, authoring standards A1–A5, A7 diff-minimality, token compliance), wired into CI as the merge bar for prompt-changing PRs.

Nothing lives here yet by design — M0 discipline (don't pull future-milestone features forward).
