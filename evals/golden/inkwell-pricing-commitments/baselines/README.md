# Baselines for inkwell-pricing-commitments

`m0-ungrounded.html` — the live run that motivated this case: it invented "All plans include a 14-day trial. No card required to start.", which no prompt phrase licensed. The `a6-no-invented-commitments` check must flag it (visible in the harness baseline report).
